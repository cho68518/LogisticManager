{
  "version": "1.0",
  "description": "송장출력_특수출력_감천분리출고 테이블 컬럼 매핑",
  "created_date": "2025-08-17",
  "last_updated": "2025-08-17",
  
  "table_info": {
    "table_name": "송장출력_특수출력_감천분리출고",
    "description": "감천분리출고 정보 테이블",
    "is_active": true,
    "processing_order": 4
  },
  
  "columns": {
    "품목코드": {
      "db_column": "품목코드",
      "data_type": "varchar(255)",
      "required": false,
      "description": "품목 코드",
      "excel_column_index": 1,
      "validation": {
        "max_length": 255
      }
    },
    "상품명": {
      "db_column": "상품명",
      "data_type": "varchar(255)",
      "required": false,
      "description": "상품명",
      "excel_column_index": 2,
      "validation": {
        "max_length": 255
      }
    },
    "수량": {
      "db_column": "수량",
      "data_type": "varchar(255)",
      "required": false,
      "description": "수량",
      "excel_column_index": 3,
      "validation": {
        "max_length": 255
      }
    },
    "택배수량": {
      "db_column": "택배수량",
      "data_type": "varchar(255)",
      "required": false,
      "description": "택배 수량",
      "excel_column_index": 4,
      "validation": {
        "max_length": 255
      }
    }
  },
  
  "business_rules": {
    "description": "감천분리출고 처리 규칙",
    "rules": [
      "모든 컬럼은 varchar(255) 타입",
      "품목코드와 상품명으로 상품 식별",
      "수량과 택배수량은 숫자 형식 권장",
      "데이터 검증 후 처리"
    ]
  },
  
  "data_quality": {
    "description": "데이터 품질 관리 규칙",
    "rules": [
      "필수 필드 검증",
      "데이터 타입 검증",
      "범위 값 검증",
      "형식 검증",
      "허용 값 검증"
    ]
  }
}
